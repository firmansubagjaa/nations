export default function Footer({ className }) {
  return (
    <div className={`bg-zinc-800 flex justify-center items-center h-screen max-h-36 ${className}`}>
      <h2 className="font-bold text-3xl">Nations</h2>
    </div>
  );
}
